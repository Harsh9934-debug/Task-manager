<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen text-white bg-zinc-900 p-10">
      <a class="text-blue-500 inline-block mb-10" href="/">Go back to Home</a>
      <% if (filename && filedata) { %>
        <h1 class="text-3xl tracking-tighter"><%= filename %></h1>
        <p class="text-xl mt-2"><%= filedata %></p>
      <% } else { %>
        <p class="text-xl text-red-500">Error: File content not available</p>
      <% } %>
    </div>
  </body>
</html>
